name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Node.js environment
        uses: actions/setup-node@v2.1.2
        with:
          node-version: 12

      - name: Install global dependencies
        run: |
          sudo apt update
          sudo apt install librsvg2-bin
          sudo npm install -g yarn
          sudo yarn global add vsce
          curl -fsSL https://code-server.dev/install.sh | sh

      - name: Install extension dependencies
        working-directory: cloverextension
        run: yarn

      - name: Build extension
        working-directory: cloverextension
        run: vsce package --yarn

      - name: Upload extension
        uses: actions/upload-artifact@v2.2.0
        with:
          name: cloverextension.vsix
          path: cloverextension/cloverextension-0.0.1.vsix

      - name: Install cloverextension dependencies
        working-directory: ide
        run: yarn

      - name: Build cloverextension
        working-directory: ide
        run: yarn prepare

      - name: Build deb of cloverextension
        working-directory: ide
        run: make deb

      - name: Upload deb
        uses: actions/upload-artifact@v2.2.0
        with:
          name: cloverextension.deb
          path: ide/deb.deb
